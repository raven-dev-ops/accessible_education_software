apiVersion: apps/v1
kind: Deployment
metadata:
  name: accessible-backend
  namespace: accessible
  labels:
    app: accessible-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: accessible-backend
  template:
    metadata:
      labels:
        app: accessible-backend
    spec:
      containers:
        - name: backend
          image: us-central1-docker.pkg.dev/cs-poc-kvjwpp97kjozemn894cmvvg/cloud-run-source-deploy/accessible-backend:latest
          ports:
            - containerPort: 8000
          env:
            - name: PORT
              value: "8000"
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"

---
apiVersion: v1
kind: Service
metadata:
  name: accessible-backend
  namespace: accessible
  labels:
    app: accessible-backend
spec:
  type: ClusterIP
  selector:
    app: accessible-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000

